{
  es-client-config: {
    uri = "https://api.etherscan.io/api"
    api-key = ${ES_API_KEY}
  }

  server-config: {
    port = 80
    port = ${?SERVER_PORT}
    host = "0.0.0.0"
    host = ${?SERVER_HOST}
  }
}
